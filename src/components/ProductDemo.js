import React from "react";
import { Button } from "@chakra-ui/react";

const ProductDemo = () => {
  return (
    <Button variant="solid" colorScheme="teal" width="200px">
      Product Demo
    </Button>
  );
};

export default ProductDemo;
